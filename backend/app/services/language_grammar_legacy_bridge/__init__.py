"""Grammar Legacy Bridge (G0) — read-only ID maps during migration."""

from __future__ import annotations

PACKAGE_VERSION = "0.1.0"
RESPONSIBILITY = (
    "Read-only map of speaking gram_* / writing structure_id / BKT grammar.* → grammar_id"
)
