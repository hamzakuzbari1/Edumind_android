"""Writing Progression (W0) — post-completion runtime contracts."""

from __future__ import annotations

PACKAGE_VERSION = "0.1.0"
RESPONSIBILITY = "Post-completion progression runtime — confidence, evidence, challenge hooks"

# Progression runs ONLY on coach-approved lesson completion — never on draft submit.
