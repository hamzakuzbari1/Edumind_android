-r requirements.txt

# Voice validation + local STT through faster-whisper.
# Required when ENABLE_WHISPER=true (default). Install: pip install -r requirements-voice.txt
imageio-ffmpeg
faster-whisper>=1.0.0
numpy>=1.24.3,<2
supertonic
